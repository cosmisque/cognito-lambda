export interface RefreshApiResponse {
    IdToken: string;
    AccessToken: string;
    error?: string;
  } 

  export interface LoginApiResponse {
    IdToken: string;
    AccessToken: string;
    error?: string
  } 